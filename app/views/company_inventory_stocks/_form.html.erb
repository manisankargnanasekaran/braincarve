<%= form_with(model: company_inventory_stock, local: true) do |form| %>
  <% if company_inventory_stock.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(company_inventory_stock.errors.count, "error") %> prohibited this company_inventory_stock from being saved:</h2>

      <ul>
      <% company_inventory_stock.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :inventory_item_id %>
    <%= form.text_field :inventory_item_id, id: :company_inventory_stock_inventory_item_id %>
  </div>

  <div class="field">
    <%= form.label :available_qty %>
    <%= form.number_field :available_qty, id: :company_inventory_stock_available_qty %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
